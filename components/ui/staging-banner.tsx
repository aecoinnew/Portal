"use client";

/**
 * Banner shown for non-production environments.
 * Reads NEXT_PUBLIC_DEPLOYMENT_ENV at build time.
 * (Staging warning banner removed for presentation polish)
 */
export function StagingBanner() {
  return null;
}
